# Task Tracker

## Code base setup
- [x] make sure Java 17 is installed
- [x] make sure Docker Engine is installed
- [x] make sure Postgres is running in the Docker
- [x] make sure `./mvnw package` runs successfully